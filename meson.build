project('my_project', 'c',
  version : '0.1',
  default_options : {
    'warning_level': '3',
    'c_std': 'c23',
    'default_library': 'static',
  }
)

# Traverse down into subdirectories
subdir('libs')
subdir('apps')
subdir('tests')
