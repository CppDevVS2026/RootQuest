project('RootQuest', 'c',
  version : '0.1.0',
  default_options : {
    'warning_level': '3',
    'c_std': 'gnu23',
    'default_library': 'static',
  }
)

# Traverse down into subdirectories
subdir('libs')
subdir('tests')
subdir('apps')
