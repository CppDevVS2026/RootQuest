character_lib = static_library('character',
  'character.c',
  include_directories : libs_includes
)

character_dep = declare_dependency(
  link_with : character_lib,
  include_directories : libs_includes
)
